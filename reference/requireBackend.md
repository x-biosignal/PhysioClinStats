# Require an optional backend or stop with guidance

Require an optional backend or stop with guidance

## Usage

``` r
requireBackend(pkg)
```

## Arguments

- pkg:

  Backend package name.

## Value

Invisibly `TRUE` if available; otherwise an error.
