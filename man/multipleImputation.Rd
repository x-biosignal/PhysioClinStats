% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation-mi.R
\name{multipleImputation}
\alias{multipleImputation}
\title{Multiply impute missing data}
\usage{
multipleImputation(
  data,
  method = "pmm",
  m = 5,
  predictors = NULL,
  seed = NULL,
  reference_based = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame with missing values (\code{NA}).}

\item{method}{Imputation method passed to mice (default \code{"pmm"});
recycled across incomplete columns.}

\item{m}{Number of imputations (default 5).}

\item{predictors}{Optional: a character vector of variables to use as
predictors for every imputed column, or a full mice
\code{predictorMatrix}. \code{NULL} uses the mice default.}

\item{seed}{Optional integer seed for reproducibility.}

\item{reference_based}{Logical; if \code{TRUE}, perform reference-based
(jump-to-reference) imputation via \pkg{rbmi} (which must be installed).}

\item{...}{Further arguments passed to \code{mice::mice}.}
}
\value{
A \code{mice} \code{"mids"} object (MAR path) with \code{m} and
\code{method} recorded.
}
\description{
Wraps \code{mice::mice} for missing-at-random multiple imputation (the
default), or delegates to \pkg{rbmi} for reference-based (jump-to-reference)
imputation. Imputations are reproducible under a fixed \code{seed}.
}
\examples{
\donttest{
if (requireNamespace("mice", quietly = TRUE)) {
  imp <- multipleImputation(mice::nhanes, m = 5, seed = 1)
}
}
}
\references{
van Buuren & Groothuis-Oudshoorn 2011 (mice); Carpenter et al.
2013 (reference-based MI); \pkg{rbmi}.
}
\seealso{
\code{\link[=poolEstimates]{poolEstimates()}}, \code{\link[=analyseEstimand]{analyseEstimand()}}
}
