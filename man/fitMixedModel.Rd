% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-mixed.R
\name{fitMixedModel}
\alias{fitMixedModel}
\title{Fit a linear mixed-effects model}
\usage{
fitMixedModel(
  data,
  formula,
  random = NULL,
  method = c("REML", "ML"),
  df = c("satterthwaite", "kenward-roger")
)
}
\arguments{
\item{data}{A data.frame in long format.}

\item{formula}{A model formula. It may carry the random-effects terms
directly (e.g. \code{y ~ x + (x | subject)}), or supply them via \code{random}.}

\item{random}{Optional one-sided random-effects formula (e.g. \code{~ (1 | subject)}) appended to \code{formula} when the latter has none.}

\item{method}{\code{"REML"} (default) or \code{"ML"}.}

\item{df}{Denominator-df method for the fixed-effect tests: \code{"satterthwaite"}
(default) or \code{"kenward-roger"} (both need \code{lmerTest}).}
}
\value{
An \code{AnalysisResult} (\code{type = "mixed_model"}) whose \code{estimate} is the
fixed-effect coefficient vector, with \code{result$fixed} (the tidy fixed-effect
table), \code{result$random} (variance components), \code{result$sigma}, \code{result$fit}
(the fitted model, for \code{\link[=estimatedMarginalMeans]{estimatedMarginalMeans()}}) and \code{result$formula}.
}
\description{
Fits a linear mixed model with \code{lme4::lmer} and returns tidy fixed-effect and
random-effect tables in an \code{AnalysisResult}. Denominator degrees of freedom
(and the associated t tests) use \code{lmerTest} when it is installed - either the
Satterthwaite or the Kenward-Roger approximation.
}
\examples{
if (requireNamespace("lme4", quietly = TRUE)) {
  data(sleepstudy, package = "lme4")
  fitMixedModel(sleepstudy, Reaction ~ Days + (Days | Subject))
}
}
\references{
Bates D, Maechler M, Bolker B, Walker S (2015). Fitting linear
mixed-effects models using lme4. Journal of Statistical Software, 67(1).
Kuznetsova A, Brockhoff PB, Christensen RHB (2017). lmerTest package.
Journal of Statistical Software, 82(13).
}
\seealso{
\code{\link[=fitMMRM]{fitMMRM()}}, \code{\link[=estimatedMarginalMeans]{estimatedMarginalMeans()}}
}
