% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty-conformal.R
\name{conformalInterval}
\alias{conformalInterval}
\title{Conformal prediction interval for a fitted model}
\usage{
conformalInterval(
  model,
  calib,
  newx,
  alpha = 0.1,
  type = c("split", "jackknife_plus"),
  seed = NULL
)
}
\arguments{
\item{model}{A fitted model supporting \code{predict(model, newdata=)} (and, for
\code{"jackknife_plus"}, \code{update(model, data=)}), e.g. an \code{lm}.}

\item{calib}{A data frame with the response and predictors: an independent
calibration set for \code{"split"}, or the full data to refit over for
\code{"jackknife_plus"}.}

\item{newx}{A data frame of predictor values to predict for.}

\item{alpha}{Miscoverage level (default 0.1 for 90\% intervals).}

\item{type}{\code{"split"} (default) or \code{"jackknife_plus"}.}

\item{seed}{Optional integer random seed recorded in the provenance.}
}
\value{
An \code{AnalysisResult} (from PhysioCore) with \code{type = "conformal"} whose
\code{result} holds \code{point}, \code{lower}, \code{upper} (one per \code{newx} row), the
\code{alpha}/\code{method}/\code{quantile}, and a provenance log capturing the seed.
}
\description{
Distribution-free predictive intervals with finite-sample marginal coverage
at least \code{1 - alpha}, requiring only exchangeability. \code{"split"} conformal
(Lei et al. 2018) scores an independent calibration set with the fitted
model and takes the conformal quantile of the absolute residuals;
\code{"jackknife_plus"} (Barber et al. 2021) refits the model leave-one-out over
the calibration data and combines the leave-one-out predictions and
residuals.
}
\details{
The coverage guarantee requires the calibration data to be exchangeable with,
and (for \code{"split"}) independent of, the data the model was trained on. When
the model uses a transformed response (e.g. \code{log(y) ~ x}) the interval is on
that transformed scale.
}
\examples{
set.seed(1)
train <- data.frame(x = rnorm(60)); train$y <- 2 * train$x + rnorm(60)
calib <- data.frame(x = rnorm(60)); calib$y <- 2 * calib$x + rnorm(60)
fit <- lm(y ~ x, data = train)
res <- conformalInterval(fit, calib, data.frame(x = c(-1, 0, 1)))
PhysioCore::resultValue(res)$lower
}
\references{
Lei, J. et al. (2018). JASA 113(523):1094-1111. Barber, R.F. et
al. (2021). Ann Statist 49(1):486-507.
}
\seealso{
\code{\link[=conformalPredict]{conformalPredict()}}, \code{\link[=bayesEstimate]{bayesEstimate()}}
}
