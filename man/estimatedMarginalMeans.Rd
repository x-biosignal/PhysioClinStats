% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-emmeans.R
\name{estimatedMarginalMeans}
\alias{estimatedMarginalMeans}
\title{Estimated marginal means}
\usage{
estimatedMarginalMeans(model, specs, contrasts = NULL, level = 0.95, ...)
}
\arguments{
\item{model}{An \code{AnalysisResult} from \code{\link[=fitMixedModel]{fitMixedModel()}} / \code{\link[=fitMMRM]{fitMMRM()}}, or a
raw model object \code{emmeans} understands.}

\item{specs}{A formula or character spec for the marginal means, e.g.
\code{~ treatment | time}.}

\item{contrasts}{Optional contrast method passed to \code{emmeans::contrast}
(e.g. \code{"pairwise"}, \code{"trt.vs.ctrl"}); \code{NULL} (default) returns means only.}

\item{level}{Confidence level for the intervals (default 0.95).}

\item{...}{Further arguments forwarded to \code{emmeans::emmeans}.}
}
\value{
An \code{AnalysisResult} (\code{type = "emmeans"}) with \code{result$emmeans} (the
marginal-mean table) and, when requested, \code{result$contrasts}.
}
\description{
Computes estimated marginal (least-squares) means from a fitted mixed or MMRM
model with \code{emmeans}, optionally with a follow-up contrast, returning the
marginal-mean table (and contrasts) in an \code{AnalysisResult}.
}
\examples{
if (requireNamespace("mmrm", quietly = TRUE) &&
    requireNamespace("emmeans", quietly = TRUE)) {
  data(fev_data, package = "mmrm")
  fit <- fitMMRM(fev_data, "FEV1", "ARMCD", "AVISIT", "USUBJID",
                 covariates = c("RACE", "SEX"))
  estimatedMarginalMeans(fit, ~ ARMCD | AVISIT)
}
}
\references{
Lenth RV (2024). emmeans: Estimated Marginal Means. R package.
}
\seealso{
\code{\link[=pairwiseContrasts]{pairwiseContrasts()}}, \code{\link[=fitMMRM]{fitMMRM()}}
}
