% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-mmrm.R
\name{fitMMRM}
\alias{fitMMRM}
\title{Fit a mixed model for repeated measures (MMRM)}
\usage{
fitMMRM(
  data,
  response,
  treatment,
  time,
  subject,
  covariates = NULL,
  covariance = c("unstructured", "ar1", "compound-symmetry", "toeplitz"),
  df = c("kenward-roger", "satterthwaite")
)
}
\arguments{
\item{data}{A long-format data.frame with one row per subject-time.}

\item{response, treatment, time, subject}{Column names of the outcome, the
treatment factor, the categorical time factor and the subject id.}

\item{covariates}{Optional character vector of extra fixed-effect covariate
columns (e.g. baseline value, stratifiers).}

\item{covariance}{Within-subject covariance: \code{"unstructured"} (default),
\code{"ar1"}, \code{"compound-symmetry"} or \code{"toeplitz"}.}

\item{df}{Denominator-df method: \code{"kenward-roger"} (default) or
\code{"satterthwaite"} (used by the \code{mmrm} path only).}
}
\value{
An \code{AnalysisResult} (\code{type = "mmrm"}) whose \code{estimate} is the
fixed-effect coefficient vector, with \code{result$coefficients} (estimate, SE,
df, t, p), \code{result$backend} (\code{"mmrm"} or \code{"gls"}), \code{result$fit} and
\code{result$formula}.
}
\description{
Fits the Mallinckrodt MMRM for a longitudinal randomised trial: fixed effects
for treatment, categorical time and their interaction (plus any covariates),
with a within-subject covariance over the repeated time points. Uses the
\code{mmrm} package when available - with Kenward-Roger or Satterthwaite adjusted
degrees of freedom - and falls back to \code{nlme::gls} otherwise. The fallback
reproduces the \code{mmrm} fixed effects for the unstructured covariance (its
primary use) and approximates the homogeneous \code{cs}/\code{ar1}/\code{toeplitz}
structures; it reports between-within (containment) degrees of freedom and
emits a message, since Kenward-Roger / Satterthwaite df need \code{mmrm}.
}
\examples{
if (requireNamespace("mmrm", quietly = TRUE)) {
  data(fev_data, package = "mmrm")
  fitMMRM(fev_data, "FEV1", "ARMCD", "AVISIT", "USUBJID",
          covariates = c("RACE", "SEX"))
}
}
\references{
Mallinckrodt CH et al. (2008). Recommendations for the primary
analysis of continuous endpoints in longitudinal clinical trials. Drug
Information Journal, 42. Sabanes Bove D et al. (2023). mmrm: Mixed Models
for Repeated Measures. R package.
}
\seealso{
\code{\link[=fitMixedModel]{fitMixedModel()}}, \code{\link[=estimatedMarginalMeans]{estimatedMarginalMeans()}}
}
