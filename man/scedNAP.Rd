% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sced-nonoverlap.R
\name{scedNAP}
\alias{scedNAP}
\title{Non-overlap of all pairs (NAP)}
\usage{
scedNAP(
  A_data,
  B_data = NULL,
  improvement = c("increase", "decrease"),
  confidence = 0.95
)
}
\arguments{
\item{A_data, B_data}{Numeric baseline (A) and intervention (B) observations,
or pass a data.frame to the phase orchestrator \code{\link[=scedABAB]{scedABAB()}}.}

\item{improvement}{\code{"increase"} (default) if higher scores are better, or
\code{"decrease"} if lower scores are the therapeutic goal.}

\item{confidence}{Confidence level for the interval (default 0.95).}
}
\value{
An \code{AnalysisResult} (\code{type = "sced_nap"}) whose \code{estimate} is the NAP,
with an analytic \code{uncertainty} interval and \code{result$se}, \code{result$p_value}.
}
\description{
The probability that a randomly chosen intervention observation exceeds a
randomly chosen baseline observation, with ties counted as one half
(Parker & Vannest 2009) - equivalent to the area under the ROC curve. The
confidence interval is the score interval of Newcombe (2006) and the standard
error the unbiased estimator, both as in SingleCaseES.
}
\examples{
scedNAP(c(20, 20, 26, 25), c(28, 25, 30, 29))
}
\references{
Parker RI, Vannest KJ (2009). An improved effect size for
single-case research: non-overlap of all pairs. Behavior Therapy, 40(4),
357-367. Newcombe RG (2006). Confidence intervals for an effect size
measure based on the Mann-Whitney statistic. Statistics in Medicine, 25.
}
\seealso{
\code{\link[=scedTau]{scedTau()}}, \code{\link[=scedTauU]{scedTauU()}}
}
