% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival-fit.R
\name{coxModel}
\alias{coxModel}
\title{Cox proportional-hazards model with a PH check}
\usage{
coxModel(
  data,
  formula = NULL,
  time = "time",
  event = "event",
  covariates = NULL,
  conf_level = 0.95,
  ph_alpha = 0.05
)
}
\arguments{
\item{data}{A data.frame.}

\item{formula}{A model formula whose response is \code{survival::Surv(time, event)}, or supply \code{time}/\code{event}/\code{covariates} instead.}

\item{time, event, covariates}{Alternative to \code{formula}: the time and event
column names and a character vector of covariate columns.}

\item{conf_level}{Confidence level (default 0.95).}

\item{ph_alpha}{Significance level for the global PH warning (default 0.05).}
}
\value{
An \code{AnalysisResult} (\code{type = "cox_model"}) whose \code{estimate} is the
hazard-ratio vector, with \code{result$coefficients} (coef, HR, SE, z, p, CI),
\code{result$ph_test} (the \code{cox.zph} table), \code{result$ph_violated} and
\code{result$fit}.
}
\description{
Fits a Cox proportional-hazards model with \code{survival::coxph}, tests the
proportional-hazards assumption with \code{survival::cox.zph}, and warns when the
global test is significant. Returns hazard ratios with confidence intervals.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  coxModel(survival::lung, time = "time", event = "status",
           covariates = c("age", "sex", "ph.ecog"))
}
}
\references{
Cox DR (1972). Regression models and life-tables. JRSS B, 34(2).
Grambsch PM, Therneau TM (1994). Proportional hazards tests and diagnostics
based on weighted residuals. Biometrika, 81(3).
}
\seealso{
\code{\link[=survivalFit]{survivalFit()}}, \code{\link[=milestoneHazard]{milestoneHazard()}}
}
