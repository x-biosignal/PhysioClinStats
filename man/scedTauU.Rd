% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sced-nonoverlap.R
\name{scedTauU}
\alias{scedTauU}
\title{Tau-U with baseline-trend correction}
\usage{
scedTauU(
  A_data,
  B_data = NULL,
  improvement = c("increase", "decrease"),
  method = c("parker", "scan"),
  confidence = 0.95
)
}
\arguments{
\item{A_data, B_data}{Numeric baseline (A) and intervention (B) observations,
or pass a data.frame to the phase orchestrator \code{\link[=scedABAB]{scedABAB()}}.}

\item{improvement}{\code{"increase"} (default) if higher scores are better, or
\code{"decrease"} if lower scores are the therapeutic goal.}

\item{method}{Denominator convention, \code{"parker"} (default) or \code{"scan"}.}

\item{confidence}{Confidence level for the interval (default 0.95).}
}
\value{
An \code{AnalysisResult} (\code{type = "sced_tau_u"}) whose \code{estimate} is Tau-U,
with \code{result$S}, \code{result$se_S}, \code{result$z}, \code{result$p_value} and the
component S values.
}
\description{
Parker, Vannest, Davis & Sauber's (2011) Tau-U combines the A-vs-B
non-overlap with a correction for baseline (phase A) trend. Two reference
conventions differ on the denominator and this function exposes both:
}
\details{
\describe{
\item{\code{method = "parker"} (default)}{matches \code{SingleCaseES::Tau_U}:
\eqn{(S_{AB} - S_{trend A}) / (m n)}, where \eqn{S_{AB}} is the A-vs-B
Kendall S and \eqn{S_{trend A}} the within-baseline Kendall S. Can exceed
1 when a baseline trend runs counter to improvement.}
\item{\code{method = "scan"}}{matches \code{scan::tau_u()}'s "A vs. B - Trend A" row:
the same S numerator over a tie-corrected pair count \eqn{D}, giving a
Kendall tau-b in [-1, 1].}
}

The Z statistic and p-value come from the tie-corrected variance of the
Kendall S numerator and are identical under both methods (they depend on S,
not the denominator), matching \code{scan}'s Z and p.
}
\examples{
scedTauU(c(20, 20, 26, 25, 22, 23), c(28, 25, 24, 27, 30, 30, 29, 28))
}
\references{
Parker RI, Vannest KJ, Davis JL, Sauber SB (2011). Combining
nonoverlap and trend for single-case research: Tau-U. Behavior Therapy,
42(2), 284-299.
}
\seealso{
\code{\link[=scedNAP]{scedNAP()}}, \code{\link[=scedTau]{scedTau()}}
}
