% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation-pool.R
\name{analyseEstimand}
\alias{analyseEstimand}
\title{Analyse an estimand under multiple imputation for dropout}
\usage{
analyseEstimand(
  data,
  estimand,
  response,
  treatment,
  time,
  subject,
  covariates = NULL,
  m = 20,
  method = "norm",
  seed = NULL,
  covariance = "unstructured"
)
}
\arguments{
\item{data}{Long-format data (subject x time rows) with the response,
treatment, time, subject, and any (subject-level) covariates.}

\item{estimand}{An \code{"estimand"} from \code{\link[=defineEstimand]{defineEstimand()}}.}

\item{response, treatment, time, subject}{Column names.}

\item{covariates}{Optional subject-level covariate column names.}

\item{m}{Number of imputations (default 20).}

\item{method}{mice method (default \code{"norm"}, appropriate for a
continuous longitudinal response).}

\item{seed}{Optional integer seed.}

\item{covariance}{MMRM covariance structure (default \code{"unstructured"}).}
}
\value{
An \code{AnalysisResult} (type \code{"estimand_analysis"}) carrying
the estimand attributes and the pooled fixed-effect table.
}
\description{
End-to-end analysis of a declared \code{\link{defineEstimand}} estimand:
multiply-impute the dropout-missing longitudinal response (in wide form, so
the within-subject correlation is respected), fit an MMRM
(\code{\link{fitMMRM}}) to each completed data set, and pool the fixed
effects with Rubin's rules (\code{\link[=poolEstimates]{poolEstimates()}}). Treatment-policy and
hypothetical strategies use the standard MAR imputation; other strategies
warn that they need a bespoke imputation model.
}
\references{
ICH E9(R1); Rubin 1987; Mallinckrodt 2008 (MMRM).
}
\seealso{
\code{\link[=defineEstimand]{defineEstimand()}}, \code{\link[=multipleImputation]{multipleImputation()}}, \code{\link[=poolEstimates]{poolEstimates()}}
}
