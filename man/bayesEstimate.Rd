% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty-bayes.R
\name{bayesEstimate}
\alias{bayesEstimate}
\title{Bayesian estimate of a mean (conjugate Normal-Normal or Stan)}
\usage{
bayesEstimate(
  y,
  data = NULL,
  prior_mean = 0,
  prior_sd = Inf,
  sigma = NULL,
  level = 0.95,
  seed = NULL
)
}
\arguments{
\item{y}{A numeric vector (analytic path) or a model \code{formula} (Stan path).}

\item{data}{Data frame for the \code{formula} path.}

\item{prior_mean, prior_sd}{Normal prior mean and SD (default 0 and \code{Inf}, a
flat prior).}

\item{sigma}{Known likelihood SD; \code{NULL} uses the sample SD of \code{y}.}

\item{level}{Credible level (default 0.95).}

\item{seed}{Optional integer random seed recorded in the provenance.}
}
\value{
An \code{AnalysisResult} (from PhysioCore) with \code{type = "bayes"} whose
\code{result} holds \code{posterior_mean}, \code{posterior_sd}, \code{ci_lower}, \code{ci_upper},
the \code{level}/\code{method}, plus a provenance log with the seed.
}
\description{
For a numeric sample, returns the analytic Normal-Normal conjugate posterior
for the mean: a normal prior \code{N(prior_mean, prior_sd^2)} combined with a
normal likelihood of known scale \code{sigma} gives a normal posterior in closed
form (a flat prior, \code{prior_sd = Inf}, reproduces the sampling posterior
\code{N(mean(y), sigma^2/n)}). For a model \code{formula} the estimation is delegated
to rstanarm/brms when installed, and errors informatively otherwise -- the
analytic numeric-vector path always runs.
}
\examples{
set.seed(1)
bayesEstimate(rnorm(30, mean = 5), prior_mean = 0, prior_sd = 10)
}
\references{
Gelman, A. et al. (2013). Bayesian Data Analysis, 3rd ed.
}
\seealso{
\code{\link[=credibleInterval]{credibleInterval()}}, \code{\link[=conformalInterval]{conformalInterval()}}
}
