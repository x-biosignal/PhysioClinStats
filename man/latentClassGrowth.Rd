% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery-lcga.R
\name{latentClassGrowth}
\alias{latentClassGrowth}
\title{Latent-class growth analysis of longitudinal trajectories}
\usage{
latentClassGrowth(
  data,
  subject,
  time,
  outcome,
  n_classes = 1:3,
  degree = 1,
  seed = NULL
)
}
\arguments{
\item{data}{A long-format data frame.}

\item{subject, time, outcome}{Column names for the grouping factor, the time
variable, and the response.}

\item{n_classes}{Integer vector of class counts to compare (default
\code{1:3}); the BIC-minimising count is selected.}

\item{degree}{Polynomial degree of the per-class growth curve (default 1,
linear).}

\item{seed}{Optional RNG seed (flexmix uses random starts).}
}
\value{
An \code{AnalysisResult} (type \code{"latent_class_growth"}) whose
\code{result} holds the selected \code{n_classes}, the \code{bic} table, the
per-subject \code{assignment}, the \code{posterior} probabilities, the
\code{entropy}, and the fitted flexmix \code{model}.
}
\description{
Fits a finite mixture of polynomial growth trajectories to panel data (each
subject belongs to one latent class), selecting the number of classes by BIC.
Returns per-subject class assignments, posterior class probabilities, and the
normalised entropy of the classification.
}
\examples{
set.seed(1)
df <- do.call(rbind, lapply(1:40, function(s) {
  fast <- s <= 20; t <- 0:6
  y <- (if (fast) 5 * t else 0.5 * t) + rnorm(7, 0, 1.5)
  data.frame(subject = s, time = t, y = y)
}))
latentClassGrowth(df, "subject", "time", "y", n_classes = 1:3)
}
\references{
Nagin 2005 (group-based trajectory modelling); Gruen & Leisch 2008
(flexmix).
}
\seealso{
\code{\link[=recoveryTrajectoryLME]{recoveryTrajectoryLME()}}, \code{\link[=proportionalRecoveryRule]{proportionalRecoveryRule()}}
}
