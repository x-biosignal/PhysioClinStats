% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival-milestone.R
\name{timeToMilestone}
\alias{timeToMilestone}
\title{Derive a time-to-milestone survival endpoint}
\usage{
timeToMilestone(
  data,
  id,
  time,
  value,
  threshold,
  direction = c("increase", "decrease"),
  censor_at = NULL
)
}
\arguments{
\item{data}{A long-format data.frame with one row per subject-visit.}

\item{id, time, value}{Column names of the subject id, the visit time and the
outcome value.}

\item{threshold}{The clinical milestone value.}

\item{direction}{\code{"increase"} (default; the milestone is reaching a value
\verb{>= threshold}) or \code{"decrease"} (a value \verb{<= threshold}).}

\item{censor_at}{Optional common administrative censoring time for
non-attainers; defaults to each subject's last observation time.}
}
\value{
A data.frame with columns \code{id}, \code{time}, \code{event} (1 attained /
0 censored) and \code{attained}, carrying a \code{Surv} object as the \code{"surv"}
attribute.
}
\description{
From a long-format longitudinal series, finds for each subject the first time
its outcome crosses a clinical \code{threshold}, producing a right-censored
\verb{(time, event)} pair per subject: \code{event = 1} at the first crossing, or
\code{event = 0} (right-censored) at the last observation - or at \code{censor_at} - for
subjects who never attain the milestone.
}
\examples{
d <- data.frame(
  id = rep(c("a", "b"), each = 3), time = rep(1:3, 2),
  value = c(10, 30, 55, 12, 20, 28))
timeToMilestone(d, "id", "time", "value", threshold = 50)
}
\seealso{
\code{\link[=survivalFit]{survivalFit()}}, \code{\link[=milestoneHazard]{milestoneHazard()}}
}
