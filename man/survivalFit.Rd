% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival-fit.R
\name{survivalFit}
\alias{survivalFit}
\title{Kaplan-Meier survival fit with attainment-time summary}
\usage{
survivalFit(
  data,
  time = "time",
  event = "event",
  group = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data.frame with the survival columns.}

\item{time, event}{Column names of the follow-up time and the event indicator
(1 = event, 0 = censored).}

\item{group}{Optional grouping-variable column name for stratified curves.}

\item{conf_level}{Confidence level (default 0.95).}
}
\value{
An object of class \code{physio_km} (a list) with \code{fit} (the \code{survfit}),
\code{quantiles} (median / Q1 / Q3 attainment times with CIs, per group) and
\code{summary} table, plus \code{print} and \code{plot} methods.
}
\description{
Fits a Kaplan-Meier estimator with \code{survival::survfit}, optionally stratified
by a grouping variable, and returns the median and quartile attainment times
with their confidence intervals.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  survivalFit(survival::lung, "time", "status", group = "sex")
}
}
\references{
Kaplan EL, Meier P (1958). Nonparametric estimation from
incomplete observations. JASA, 53(282), 457-481.
}
\seealso{
\code{\link[=coxModel]{coxModel()}}, \code{\link[=timeToMilestone]{timeToMilestone()}}
}
