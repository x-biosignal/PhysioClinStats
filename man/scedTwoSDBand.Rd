% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sced-band.R
\name{scedTwoSDBand}
\alias{scedTwoSDBand}
\title{Two-standard-deviation band decision rule}
\usage{
scedTwoSDBand(
  A_data,
  B_data = NULL,
  improvement = c("increase", "decrease"),
  k = 2,
  consecutive = 2L
)
}
\arguments{
\item{A_data, B_data}{Numeric baseline and intervention observations.}

\item{improvement}{\code{"increase"} (default) or \code{"decrease"}; sets which side of
the band counts as improvement.}

\item{k}{Band half-width in baseline SDs (default 2).}

\item{consecutive}{Number of successive out-of-band points that trigger the
flag (default 2, Nelson's rule).}
}
\value{
An \code{AnalysisResult} (\code{type = "sced_2sd"}) whose \code{estimate} is the
logical decision, with \code{result$mean}, \code{result$sd}, \code{result$upper},
\code{result$lower}, \code{result$outside} (per-B-point side: -1/0/+1) and
\code{result$first_run_at} (index of the first triggering point, or \code{NA}).
}
\description{
Draws the Nelson (1984) 2-SD band from the baseline (A) phase - the baseline
mean plus and minus two baseline standard deviations - and flags the
intervention (B) phase for a systematic shift: the rule fires when a run of
\code{consecutive} or more successive B points falls on the same side beyond the
band.
}
\examples{
scedTwoSDBand(c(10, 12, 11, 9, 10), c(15, 16, 17, 16, 18))
}
\references{
Nelson LS (1984). The Shewhart control chart - tests for special
causes. Journal of Quality Technology, 16(4), 237-239. Applied to SCED in
Gast & Ledford (2014).
}
\seealso{
\code{\link[=scedCelerationLine]{scedCelerationLine()}}, \code{\link[=scedABAB]{scedABAB()}}
}
