% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose-response.R
\name{doseResponse}
\alias{doseResponse}
\title{Therapy dose-response model}
\usage{
doseResponse(
  data,
  outcome,
  dose,
  covariates = NULL,
  subject = NULL,
  baseline = NULL,
  form = c("linear", "log", "spline", "emax"),
  df = 3,
  n_grid = 50
)
}
\arguments{
\item{data}{A \code{data.frame} with the columns named below.}

\item{outcome}{Name of the outcome column.}

\item{dose}{Name of the therapy-dose column (hours / repetitions / sessions).}

\item{covariates}{Optional character vector of confounder columns to adjust
for (added linearly).}

\item{subject}{Optional subject-id column; when given, a random intercept per
subject is fit via \code{\link[=fitMixedModel]{fitMixedModel()}} (needs \code{lme4}).}

\item{baseline}{Optional baseline column; when given it is added as an
adjustment covariate (ANCOVA-style change analysis).}

\item{form}{Dose form: \code{"linear"}, \code{"log"} (needs positive dose),
\code{"spline"} (natural cubic spline, \code{df} terms) or \code{"emax"}
(\code{E0 + Emax*dose/(ED50+dose)}, fit by \code{\link[stats:nls]{stats::nls()}}; no subject/covariates).}

\item{df}{Spline degrees of freedom for \code{form = "spline"} (default 3).}

\item{n_grid}{Number of dose points in the predicted curve (default 50).}
}
\value{
A \link[PhysioCore:AnalysisResult]{PhysioCore::AnalysisResult} of \code{type = "dose_response"}: \code{estimate}
is the dose effect (slope, spline coefficients, or \code{E0}/\code{Emax}/\code{ED50});
\code{result} holds \code{coefficients}, the \code{curve} (\code{dose}, \code{predicted}), the \code{fit}
and the \code{form}.
}
\description{
Fits an outcome (or change from baseline) as a function of a continuous /
ordinal therapy dose, with optional confounder adjustment, subject random
intercepts and a non-linear dose form. Returns the fitted dose effect, a
predicted dose-response curve and the coefficient table.
}
\examples{
set.seed(1)
d <- data.frame(dose = rep(c(0, 5, 10, 20), each = 15))
d$change <- 0.4 * d$dose + rnorm(nrow(d), 0, 2)
dr <- doseResponse(d, outcome = "change", dose = "dose", form = "linear")
PhysioCore::resultValue(dr)$coefficients
}
\references{
Ruberg SJ (1995) Dose response studies. \emph{J Biopharm Stat} 5:1-14.
}
\seealso{
\code{\link[=fitMixedModel]{fitMixedModel()}}, \code{\link[=proportionalRecoveryRule]{proportionalRecoveryRule()}}
}
