% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{defineEstimand}
\alias{defineEstimand}
\title{Define an ICH E9(R1) estimand}
\usage{
defineEstimand(
  treatment,
  population,
  endpoint,
  intercurrent = list(event = NA_character_, strategy = "treatment-policy"),
  summary_measure = "difference in means"
)
}
\arguments{
\item{treatment}{The treatment condition(s) being compared.}

\item{population}{The target population.}

\item{endpoint}{The endpoint / variable of interest.}

\item{intercurrent}{A named list with \code{event} (the intercurrent event)
and \code{strategy} (one of \link{ESTIMAND_STRATEGIES}).}

\item{summary_measure}{The population-level summary (e.g.
\code{"difference in means"}).}
}
\value{
An \code{"estimand"} object (a named list of the five attributes plus
the strategy's analysis \code{recipe}).
}
\description{
Constructs a validated estimand from its five ICH E9(R1) attributes. The
returned object is a fully-named list (class \code{"estimand"}) that
round-trips through the \code{estimand} slot of an
\code{\link[PhysioCore]{AnalysisResult}}.
}
\examples{
defineEstimand(
  treatment = "rehab protocol A vs B", population = "post-stroke",
  endpoint = "6-month gait speed",
  intercurrent = list(event = "treatment discontinuation",
                      strategy = "treatment-policy"),
  summary_measure = "difference in means")
}
\references{
ICH E9(R1) addendum on estimands and sensitivity analysis (2019).
}
\seealso{
\code{\link[=analyseEstimand]{analyseEstimand()}}, \code{\link[=multipleImputation]{multipleImputation()}}
}
