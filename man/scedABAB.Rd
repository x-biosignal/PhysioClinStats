% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sced-phase.R
\name{scedABAB}
\alias{scedABAB}
\title{Analyse an ABAB (reversal) single-case design}
\usage{
scedABAB(
  x,
  phases = c("A1", "B1", "A2", "B2"),
  contrasts = list(c("A1", "B1"), c("A2", "B2")),
  improvement = c("increase", "decrease"),
  value = NULL,
  phase = NULL,
  time = NULL,
  assay = NULL,
  channel = 1L
)
}
\arguments{
\item{x}{A \code{PhysioExperiment} single-channel series, or a
\code{data.frame(time, value, phase)}. Observations are ordered by \code{time} before
analysis, so a \code{time} column need not be pre-sorted; without one, row order
is used. Ordering matters because the Tau-U baseline-trend correction and
the 2-SD consecutive-run rule are sequence-dependent.}

\item{phases}{Ordered character vector of the phase labels (default
\code{c("A1","B1","A2","B2")}).}

\item{contrasts}{A list of length-2 \code{c(baseline, intervention)} phase-label
pairs (default the two reversal contrasts).}

\item{improvement}{\code{"increase"} (default) or \code{"decrease"}.}

\item{value, phase, time, assay, channel}{Column/assay selectors passed to the
extractor when \code{x} is a data.frame or PhysioExperiment.}
}
\value{
An object of class \code{sced_abab} (a list) with \code{phase_data},
\code{contrasts} (per contrast: NAP/Tau/Tau-U/PND/PEM results, the 2-SD decision
and both phase celeration lines) and a \code{summary} data.frame, with \code{print}
and \code{plot} methods.
}
\description{
Orchestrates the SCED analysis of a reversal design: it separates the phases
(by default the four phases \verb{A1, B1, A2, B2}), and for each intervention
contrast (\code{B1} vs \code{A1} and \code{B2} vs \code{A2}) computes the non-overlap effect
sizes, the 2-SD-band decision and per-phase celeration lines.
}
\examples{
df <- data.frame(
  value = c(10, 11, 9, 18, 19, 20, 10, 12, 11, 21, 22, 20),
  phase = rep(c("A1", "B1", "A2", "B2"), each = 3))
res <- scedABAB(df)
res
}
\seealso{
\code{\link[=scedNAP]{scedNAP()}}, \code{\link[=scedTwoSDBand]{scedTwoSDBand()}}, \code{\link[=scedCelerationLine]{scedCelerationLine()}}
}
