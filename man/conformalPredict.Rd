% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty-conformal.R
\name{conformalPredict}
\alias{conformalPredict}
\title{Conformal prediction band for a single new patient}
\usage{
conformalPredict(model, calib, newx, alpha = 0.1, seed = NULL)
}
\arguments{
\item{model}{A fitted model (see \code{\link[=conformalInterval]{conformalInterval()}}).}

\item{calib}{The calibration data frame (response + predictors).}

\item{newx}{A one-row data frame of predictors for the new patient.}

\item{alpha}{Miscoverage level (default 0.1).}

\item{seed}{Optional integer random seed.}
}
\value{
An \code{AnalysisResult} with \code{type = "conformal"} for the single patient.
}
\description{
Convenience wrapper around split-conformal \code{\link[=conformalInterval]{conformalInterval()}} for one new
observation, returning the guaranteed-coverage predicted band.
}
\examples{
set.seed(1)
d <- data.frame(x = rnorm(80)); d$y <- 1.5 * d$x + rnorm(80)
fit <- lm(y ~ x, data = d[1:40, ])
conformalPredict(fit, d[41:80, ], data.frame(x = 0.5))
}
\seealso{
\code{\link[=conformalInterval]{conformalInterval()}}
}
