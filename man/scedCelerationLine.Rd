% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sced-band.R
\name{scedCelerationLine}
\alias{scedCelerationLine}
\title{Celeration (trend) line for a single-case phase}
\usage{
scedCelerationLine(
  value,
  time = NULL,
  method = c("split_middle", "ols"),
  ratio_period = 7
)
}
\arguments{
\item{value}{Numeric phase observations, or pass \code{time}/\code{value} explicitly.}

\item{time}{Optional numeric session index (default \code{seq_along(value)}).}

\item{method}{\code{"split_middle"} (default) or \code{"ols"}.}

\item{ratio_period}{Sessions spanned by the reported celeration ratio
(default 7, a weekly ratio for daily data).}
}
\value{
An \code{AnalysisResult} (\code{type = "sced_celeration"}) whose \code{estimate} is
the slope (celeration per session), with \code{result$intercept},
\code{result$fitted}, \code{result$bounce} (the ratio of the largest positive to the
largest negative residual magnitude) and \code{result$celeration_ratio} (the
multiplicative change over \code{ratio_period} sessions).
}
\description{
Fits a trend line to one phase, either by the White & Haring split-middle
method (the standard SCED hand method) or by ordinary least squares.
}
\examples{
scedCelerationLine(c(4, 6, 5, 8, 7, 10, 9))
}
\references{
White OR, Haring NG (1980). Exceptional Teaching, 2nd ed.
Columbus, OH: Merrill.
}
\seealso{
\code{\link[=scedTwoSDBand]{scedTwoSDBand()}}, \code{\link[=scedABAB]{scedABAB()}}
}
