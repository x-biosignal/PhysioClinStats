% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal-mediation.R
\name{causalMediation}
\alias{causalMediation}
\title{Model-based causal mediation analysis}
\usage{
causalMediation(
  model_m,
  model_y,
  treat,
  mediator,
  control.value = 0,
  treat.value = 1,
  covariates = NULL,
  sims = 1000L,
  boot = FALSE,
  boot.ci.type = "perc",
  conf.level = 0.95,
  robustSE = FALSE,
  sensitivity = TRUE,
  seed = NULL,
  ...
)
}
\arguments{
\item{model_m, model_y}{Supported fitted mediator and outcome models, or
\code{AnalysisResult} objects whose \code{result$fit} contains those models.}

\item{treat, mediator}{Treatment and mediator variable names.}

\item{control.value, treat.value}{Treatment contrast supplied to the backend.}

\item{covariates}{Optional named list fixing covariates for a conditional
mediation estimand.}

\item{sims}{Positive integer number of simulations or bootstrap replicates.}

\item{boot}{Use the nonparametric bootstrap.}

\item{boot.ci.type}{Bootstrap confidence-interval type.}

\item{conf.level}{Confidence level in \verb{(0, 1)}.}

\item{robustSE}{Request heteroskedasticity-consistent uncertainty for
supported \code{lm}/\code{glm} models.}

\item{sensitivity}{Run \code{\link[mediation:medsens]{mediation::medsens()}} for an all-linear model pair.}

\item{seed}{Optional integer seed. The caller's global RNG state is restored.}

\item{...}{Further arguments passed to \code{\link[mediation:mediate]{mediation::mediate()}}.}
}
\value{
An \code{AnalysisResult} with a complete ACME/ADE/total/proportion table,
the backend object, settings, formulas, assumptions, and sensitivity
diagnostics.
}
\description{
Calls \code{\link[mediation:mediate]{mediation::mediate()}} after checking that the mediator and outcome
models describe the same ordered observations. Estimates rely on sequential
ignorability, consistency, positivity, absence of a treatment-induced
mediator-outcome confounder, and correct mediator and outcome models. An
indirect association, even when statistically significant, is not evidence
that a biological mechanism has been established.
}
